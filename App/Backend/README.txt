---------------------------------
Inicio servidor python FASTApi
python uvicorn main:app --reload

#servidor basico python apra ejecutar html python -m http.server 8080(tengo quye estar en la carpeta donde este el archivo html)

#pipenv shell - iniciar entorno virtual en local